<br>
<%= button_to "Back to Artist Page", artist_path(@artist), method: :get %>
<br>
<h1>Songs by <%= @artist.name %></h1>
<ul><% @artist.songs.each do |song| %>
        <li><%= link_to song.name, artist_song_path(@artist, song) %></li>
        <br>
    <% end %>
</ul>